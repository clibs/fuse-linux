{
  "name": "fuse-linux",
  "version": "fuse-3.6.2",
  "repo": "clibs/fuse-linux",
  "description": "The reference implementation of the Linux FUSE (Filesystem in Userspace) interface",
  "keywords": [ "libfuse", "fuse", "linux" ],
  "makefile": "clib-install.sh",
  "install": "${SHELL:-sh} clib-install.sh"
}
